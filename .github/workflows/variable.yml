name: variablr-prac
on: workflow_dispatch
jobs:
  variable:
    runs-on: ubuntu-latest
    steps:
      - name: prac-var
        run: echo hello, "${{vars.name}}"
              echo hello, "${{secrets.pass}}"
